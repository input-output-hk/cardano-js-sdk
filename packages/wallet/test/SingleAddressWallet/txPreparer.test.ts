// import { createTestScheduler } from '@cardano-sdk/util-dev';

describe('SingleAddressWallet/txPreperer', () => {
  it('delays building tx on epoch rollover, until rewards are re-fetched', () => {
    // createTestScheduler().run(({ hot, expectObservable }) => {});
  });
});
