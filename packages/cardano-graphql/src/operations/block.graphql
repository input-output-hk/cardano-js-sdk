query BlocksByHashes($hashes: [String!]!) {
  queryBlock(filter: { hash: { in: $hashes } }) {
    slot {
      number
      date
      slotInEpoch
    }
    issuer {
      id
      vrf
    }
    size
    transactionsAggregate {
      count
    }
    totalOutput
    totalFees
    epoch {
      number
    }
    hash
    blockNo
    previousBlock {
      hash
    }
    nextBlock {
      hash
    }
    confirmations
  }
}
